# Activate Python virtual environment
source .venv/bin/activate

# Switch Node version
[ -f .nvmrc ] && nvm use

# Load local environment variables from .env
dotenv

# Set shared defaults (can be overridden by .env)
# export PYTHONPATH="$PWD/src:$PYTHONPATH"
