# Alignment

Drift-triage report produced by **ALIGN** sessions. Compares the docs (`ROADMAP`, `features/`,
`SESSION`, `DECISIONS`) against reality (source code, actual state), then lists discrepancies to fix
with the user.

This is a working/temporary file: items get dispatched (fixed, deferred, or moved to `BACKLOG`),
then removed. When empty, that means docs and reality agree.

*Last run: never (spine re-based onto .fresh 2026-07-25). Empty — docs and reality agree, because
nothing has been moved yet.*

---

